"modules-left": ["hyprland/workspaces"],

"hyprland/workspaces": {
    "format": "{name}",       // Shows the workspace number (1, 2, 3...)
    "on-click": "activate",   // Click to switch
    "all-outputs": true,      // Show all workspaces on all monitors
    "sort-by-number": true
}

" Minimal Vifm configuration with modular structure

" ======================
" Main vifmrc (~/.config/vifm/vifmrc)
" ======================

" Use NeoVim as the editor
set vicmd=nvim

" Use internal syscalls for faster ops
set syscalls

" Trash support
set trash

" Sorting
set sortnumbers
set history=100

" Save state between sessions
set vifminfo=dhistory,savedirs,chistory,state,tui,shistory,phistory,fhistory,dirstack,registers,bookmarks,bmarks

" Load modules
source ~/.config/vifm/filetypes.vifm
source ~/.config/vifm/keys.vifm
source ~/.config/vifm/themes.vifm

" Add fd for find alternative
" %a is the argument: In which directory to search
" fd's -E is short for for --exclude
command! FZFfind :set noquickview | :execute 'goto' fnameescape(term('fd . %a --hidden -E .git -E .stversions | fzf 2> /dev/tty'))

" Ctrl+/ → fuzzy find in current dir (recursive), `/` is sent as `_` by many terminals.
nnoremap <C-_> :FZFfind .<cr>
